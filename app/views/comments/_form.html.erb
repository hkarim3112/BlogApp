<%= form_for [commentable, comment], remote: true do |form|%>
  <div class="field">
    <%= form.trix_editor :content, required: true %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
